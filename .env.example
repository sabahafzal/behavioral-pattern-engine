# Copy this file to .env for local development.
# Comma-separated list of valid API keys — generate real random keys for
# production, don't reuse the dev placeholder.
API_KEYS=dev-local-key
ENVIRONMENT=development
